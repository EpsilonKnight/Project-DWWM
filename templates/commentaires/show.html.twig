{% extends 'base.html.twig' %}

{% block title %}Commentaires{% endblock %}

{% block body %}
    <h1>Commentaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ commentaire.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaires_index') }}">Retour</a>

    <a href="{{ path('app_commentaires_edit', {'id': commentaire.id}) }}">Modifier</a>

    {{ include('commentaires/_delete_form.html.twig') }}
{% endblock %}
