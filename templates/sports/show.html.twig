{% extends 'base.html.twig' %}

{% block title %}Sports{% endblock %}

{% block body %}
    <h1>Sports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sport.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sport.nom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ sport.imageName }}</td>
            </tr>
            
            
        </tbody>
    </table>

    <a href="{{ path('app_sports_index') }}">back to list</a>

    <a href="{{ path('app_sports_edit', {'id': sport.id}) }}">edit</a>

    {{ include('sports/_delete_form.html.twig') }}
{% endblock %}
