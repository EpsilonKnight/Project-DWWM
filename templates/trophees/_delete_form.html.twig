<form method="post" action="{{ path('app_trophees_delete', {'id': trophee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trophee.id) }}">
    <button class="btn">Delete</button>
</form>
